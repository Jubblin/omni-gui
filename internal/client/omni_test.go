package client

// Note: Tests for NewOmniClient are removed because the function
// falls back to settings file when environment variables are not set,
// making it difficult to test in isolation without mocking file system access.

